[Unit]
Description=arc-inv-scan — probe the inventory register + discover unregistered assets

[Service]
Type=oneshot
ExecStart=%h/.local/bin/arc-inv scan --quiet
Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin
TimeoutStartSec=240
Nice=10
