[Unit]
Description=arc-vpn-watch — fast reconnect check (60s cadence, not arc-heal's 3h)

[Service]
Type=oneshot
ExecStart=%h/.local/bin/arc-vpn-watch
TimeoutStartSec=150   # a hung protonvpn must never leave this oneshot 'activating' forever
